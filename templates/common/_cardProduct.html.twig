<div class="w-100">
    <img src="{{ asset('build/images/')}}">
</div>
<h4>{{ product.label }}</h4>
<p>{{ product.description|slice(0,20) ~ '...' }}</p>
<p>note</p>
<strong>{{ product.price }}</strong>
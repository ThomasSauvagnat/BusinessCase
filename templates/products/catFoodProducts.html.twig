{% extends 'base-front.html.twig' %}

{% block title %}Nourriture pour chat{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <h1>Nourriture pour chats</h1>
            {% for product in catFoodProducts %}
                <div class="col-lg-3">
                    {% include ('common/_cardProduct.html.twig') %}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}

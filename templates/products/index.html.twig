{% extends 'base-front.html.twig' %}

{% block title %}Produits pour chien{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="row">
            <h1>Produits pour chiens</h1>
            {% for product in dogProducts %}
                <div class="col-lg-3">
                    {% include ('common/_cardProduct.html.twig') %}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
